# Changelog

## v3.4 (2026-01-24)

### 🚀 Frontend (Scanner)
- **Parallel Protocol Check:** Теперь сканер проверяет доступность IP одновременно по протоколам `http://` (порт 80) и `https://` (порт 443).
  - Если *любой* из протоколов отвечает успешно — IP считается доступным.
  - Это решает проблему ложно-отрицательных результатов для серверов с невалидными SSL-сертификатами (common name mismatch), которые ранее отбрасывались.
- **Strict Error Handling:** Исправлен критический баг, из-за которого некоторые сетевые ошибки (кроме таймаута) могли ошибочно считаться "успехом". Теперь `Connection Refused` или `Reset` корректно помечаются как `Fail`.
- **UI:** Добавлена кнопка "📊 Отчет" для скачивания статистики.

### 🧠 Backend (API & Logic)
- **Smart Splitting:** Внедрена логика автоматической нарезки больших подсетей (шире `/24`) на сегменты `/24` при импорте.
- **Priority Management:** Новым подсетям теперь автоматически присваивается высокий приоритет (`10`) для ускоренного первичного сканирования.
- **CSV Reporting:** Добавлен эндпоинт `/api/report/csv` для выгрузки детальной статистики за 72 часа.
  - Поля: `provider`, `cidr`, `region`, `isp`, `total_tests`, `success_count`, `avg_latency`.
- **Security:** Скачивание отчетов теперь защищено проверкой `Mirror ID`. Доступ разрешен только активным нодам (проверка регистра отключена для удобства).
- **Maintenance:** Добавлена задача (Cron) для автоматического пересканирования "старых" сетей раз в 10 дней.

### 🛠 Tools
- **Node Generator:** Полностью переписан генератор команд запуска (`admin_generator.html`). Исправлены ошибки экранирования bash-команд.
- **Docker:** Внедрено монтирование исходного кода (`volumes`) для удобства разработки и горячего обновления бэкенда.
