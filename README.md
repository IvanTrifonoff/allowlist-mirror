# 📡 Allowlist Scanner (Distributed Mirror System)

**Инструмент распределенного тестирования доступности подсетей ("Белых списков") в РФ.**

Проект позволяет выявлять IP-диапазоны, доступные через мобильных операторов и провайдеров РФ вопреки блокировкам, используя сеть распределенных зеркал (Proxy Nodes).

---

## 🚀 Хотите помочь проекту? Запустите Зеркало!

Если у вас есть **VPS/VDS или сервер в России** (особенно с "белым" IP или в дата-центрах, где наблюдается доступность заблокированных ресурсов), вы можете поднять **Зеркало**.

Зеркало — это легкий Docker-контейнер (Nginx), который раздает фронтенд пользователям и проксирует их запросы на наш центральный аналитический сервер.

### 📋 Требования
*   Любой Linux сервер (Ubuntu/Debian/CentOS).
*   Установленный [Docker](https://docs.docker.com/get-docker/).
*   Свободный порт **80** (или любой другой, например 8080).
*   **Важно:** Порт 443 не требуется (не конфликтует с VLESS/Xray).

### 🔑 Шаг 1. Получение доступа (Регистрация Зеркала)
Для защиты от спама и атак, зеркала работают по авторизации.
Чтобы получить `MIRROR_AUTH_TOKEN`, выполните одно из действий:

1.  Создайте **Issue** в этом репозитории с тегом `Request Mirror Token`.
2.  Или напишите администратору в Telegram (контакт: *@ваш_ник*).

Укажите название вашего хостинга/провайдера.

### 🐳 Шаг 2. Запуск (Docker)

Выполните одну команду на вашем сервере.
Замените `ВАШ_ТОКЕН` на полученный ключ, а `ИМЯ_ЗЕРКАЛА` на любое удобное название (латиницей).

```bash
# Вариант A: Сборка из исходников (Рекомендуется)
docker build -t allowlist-mirror https://github.com/IvanTrifonoff/allowlist-mirror.git
docker run -d \
  --restart=always \
  -p 80:80 \
  -e BACKEND_URL="http://82.202.141.81:8001" \
  -e MIRROR_AUTH_TOKEN="ВСТАВИТЬ_ПОЛУЧЕННЫЙ_ТОКЕН" \
  -e MIRROR_ID_NAME="my_vps_name" \
  --name allowlist-mirror \
  allowlist-mirror

# Вариант Б: Использование готового образа (Если опубликован)
# docker run -d ... ghcr.io/ivantrifonoff/allowlist-mirror:latest
```

