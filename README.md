# 📡 Allowlist Scanner (Mirror Node)

Это фронтенд-узел (зеркало) для системы **Allowlist Scanner**. 
Он позволяет пользователям проверять доступность сетевых ресурсов с их локального соединения и отправлять результаты на центральный сервер.

## 🚀 Как это работает

1. Пользователь открывает страницу в браузере.
2. Скрипт анализирует сетевое окружение пользователя для определения пригодности к сканированию.
3. Если окружение подходит, скрипт запрашивает список целевых подсетей у Бэкенда.
4. Браузер пытается подключиться к целевым IP (через `fetch` в режиме `no-cors`).
5. Результаты (доступен/недоступен + задержка) отправляются обратно на Бэкенд.

## 📊 Статистика

В нижней части страницы отображается глобальная статистика проекта в реальном времени:
* **Активные подсети:** Количество диапазонов IP, находящихся в очереди на проверку.
* **Проверок:** Общее число тестов, выполненных всеми участниками.
* **Топ провайдеров:** Рейтинг хостингов/провайдеров, чьи IP-адреса оказались доступны ("Белые списки").

## 🤝 Добавление провайдеров (Data Contribution)

Мы ведем открытый реестр подсетей для сканирования. Если вы хотите предложить диапазоны IP для проверки (например, облачные провайдеры или хостинги), вы можете сделать это через GitHub.

👉 **Репозиторий данных: [IvanTrifonoff/allowlist-data](https://github.com/IvanTrifonoff/allowlist-data)**

1. Сделайте Fork репозитория.
2. Добавьте запись в файл `providers.json` в следующем формате:
   ```json
   {
     "provider": "Название Провайдера",
     "cidr": "10.20.30.0/24",
     "priority": 5,
     "comment": "Дополнительная информация"
   }
   ```
3. Отправьте **Pull Request**. После проверки и слияния, данные автоматически загрузятся в систему в течение часа.

## 🛠 Установка и Запуск (Docker)

Самый простой способ запустить зеркало — использовать Docker.

### Требования
* Docker
* Доступ к интернету

### Быстрый старт

```bash
# 1. Склонируйте репозиторий
git clone https://github.com/IvanTrifonoff/allowlist-mirror.git
cd allowlist-mirror

# 2. Соберите образ
docker build -t allowlist-mirror .

# 3. Запустите контейнер
# Замените BACKEND_URL на адрес вашего координационного сервера
docker run -d \
  -p 80:80 \
  -e BACKEND_URL="http://82.202.141.81:8001" \
  --name allowlist-mirror \
  allowlist-mirror
```

После запуска откройте `http://localhost` в браузере.

## ⚙️ Конфигурация

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BACKEND_URL` | URL API сервера (координатора) | *Обязательно* |

## 📦 Структура проекта

* `Dockerfile` — Сборка Nginx на базе Alpine.
* `nginx.conf` — Конфигурация проксирования (раздает статику и проксирует `/api` на Бэкенд).
* `html/index.html` — Одностраничное приложение (SPA) с логикой проверки и статистикой.

## 🛡 Безопасность

* Приложение **не** собирает личные данные.
* Система анализирует сетевое окружение. Если оно не соответствует критериям для сбора данных, сканирование **автоматически отключается**, чтобы не засорять базу данных некорректными данными.

---
License: MIT
