# 📡 Allowlist Scanner: Mirror Node

Клиентская часть (фронтенд) для распределенной системы мониторинга доступности сетевых ресурсов.
Работает как SPA (Single Page Application) внутри Nginx контейнера.

## 📋 Требования

*   **OS:** Linux, macOS, Windows.
*   **Runtime:** Docker Engine.
*   **Сеть:** Исходящий доступ к HTTP/HTTPS (порты 80/443).

## 🚀 Быстрый старт

Запустите одной командой. Контейнер автоматически удалится после остановки (`--rm`).

```bash
docker run -d --rm \
  -p 80:80 \
  -e BACKEND_URL="http://82.202.141.81:8001" \
  --name allowlist-mirror \
  ghcr.io/ivantrifonoff/allowlist-mirror:latest 2>/dev/null || \
docker build -t allowlist-mirror https://github.com/IvanTrifonoff/allowlist-mirror.git && \
docker run -d --rm \
  -p 80:80 \
  -e BACKEND_URL="http://82.202.141.81:8001" \
  --name allowlist-mirror \
  allowlist-mirror
```

*При успешном запуске интерфейс доступен по адресу: [http://localhost](http://localhost)*

## ⚙️ Конфигурация

Переменные окружения для `docker run`:

| Переменная | Обязательно | Описание | Пример |
|------------|-------------|----------|--------|
| `BACKEND_URL` | **Да** | Адрес API координатора. | `http://82.202.141.81:8001` |
| `PORT` | Нет | Порт Nginx (внутренний). | `80` |

## 🛡 Алгоритм работы и Приватность

1.  **Environment Check:** При загрузке скрипт проверяет доступность контрольных узлов (Google/Yandex) через `fetch` с таймаутом 5с.
    *   Если "Глобальный Интернет" доступен -> **Тест отменяется**.
    *   Если "Глобальный Интернет" недоступен -> **Запуск сканера**.
2.  **Fetch Tasks:** Запрос списка целевых подсетей (`/api/tasks`) у координатора.
3.  **Probe:** Попытка соединения с целевыми IP (режим `no-cors`). Измеряется RTT (Latency).
4.  **Submit:** Отправка результатов (`true/false` + `ms`) на сервер.

**Политика данных:**
*   Никакие персональные данные (Cookie, LocalStorage) не сохраняются.
*   Сервер видит только IP-адрес, с которого пришел запрос (IP зеркала).

## 🔧 Траблшутинг

**Проблема:** "Сервер не вернул задач" или ошибки CORS.
**Решение:**
1.  Откройте DevTools (F12) -> Console.
2.  Убедитесь, что `BACKEND_URL` доступен с вашего IP:
    ```bash
    curl -I http://82.202.141.81:8001/api/tasks
    ```
3.  Если зеркало запущено на HTTPS, а бэкенд на HTTP — браузер заблокирует запрос (Mixed Content). Запускайте зеркало локально на HTTP.

---
**Разработка:**
*   Repo: [IvanTrifonoff/allowlist-mirror](https://github.com/IvanTrifonoff/allowlist-mirror)
*   Data Registry: [IvanTrifonoff/allowlist-data](https://github.com/IvanTrifonoff/allowlist-data)